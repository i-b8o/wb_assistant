import 'package:flutter/material.dart';

class ConfirmEmailLarge extends StatelessWidget {
  const ConfirmEmailLarge({Key? key, required this.onPress}) : super(key: key);
  final void Function() onPress;
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
