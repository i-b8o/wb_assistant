import 'package:flutter/material.dart';

class WaitLoadingDetailsPage extends StatelessWidget {
  const WaitLoadingDetailsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    print("WaitLoadingDetailsPage");
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
